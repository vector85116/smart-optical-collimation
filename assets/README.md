# Assets

Place selected project media here:

- `system_photo.jpg`: full hardware setup.
- `demo.gif`: short closed-loop alignment demonstration.
- `result_screenshot.jpg`: MaixCAM overlay result.

Avoid committing large raw videos. The `.gitignore` excludes common video formats by default.
