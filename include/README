这个目录用于存放项目头文件。

头文件通常包含 C/C++ 声明、宏定义、枚举、结构体和函数接口，供 `src/` 目录中的多个源文件共同使用。

在本项目中，`EmmV5.h` 定义了 EMM V5 电机驱动器相关的协议接口，例如：

- 使能电机。
- 位置模式控制。
- 速度模式控制。
- 立即停止。
- 读取驱动器参数。
- 回零相关操作。

在源文件中可以通过下面方式引用头文件：

```cpp
#include "EmmV5.h"
```

把公共接口放在头文件中，可以避免在多个源文件里重复声明同一批函数，也方便后续维护电机驱动协议。
