这个目录用于存放项目私有库。

PlatformIO 会自动扫描 `lib/` 目录中的库，并在构建时将它们编译、链接到最终固件中。

推荐的目录结构如下：

```text
lib/
└── your_library_name/
    ├── src/
    │   ├── your_library.cpp
    │   └── your_library.h
    └── library.json
```

当前项目的电机驱动代码暂时直接放在 `src/EmmV5.cpp` 和 `include/EmmV5.h` 中。如果后续电机协议、控制算法或串口通信模块继续变大，可以再拆分到 `lib/` 目录中，形成独立库。
