这个目录用于存放 PlatformIO 测试代码。

单元测试可以用来验证项目中的独立模块，例如：

- 串口指令解析是否正确。
- `missing` 搜索状态机是否按预期推进。
- 电机组选择逻辑是否正确。
- 上位机方向指令与下位机解析规则是否一致。

当前项目还没有正式测试用例。后续如果把指令解析和状态机拆成更独立的模块，可以在这里加入 PlatformIO 测试，减少调参过程中引入回归问题的风险。
